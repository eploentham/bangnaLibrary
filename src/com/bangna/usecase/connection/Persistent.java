package com.bangna.usecase.connection;import java.text.DecimalFormat;//import com.hospital_os.utility.*;public class Persistent implements Cloneable, java.io.Serializable{   private String object_id;   private String log_detail;   public String pk_field;   public String table;   public static String hospital_site;   /**    * @roseuid 3F61520fa30161    */   public Persistent(){   }   public void setObjectId(String str){ object_id = str; }   public void setLogDetail(String str){ log_detail = str; }      public String getObjectId() {        return object_id;    }      public String getLogDetail() { return log_detail; }      public void setHospitalSiteId(String str) {       this.hospital_site = str;          }        public void generateOID(String id)    {        object_id = getGenID(id);          }      public void generateStandardOID(String id) {       object_id = getGenStandardID(id);   }   public void generateOID(int i) {       long time=System.currentTimeMillis();       object_id = String.valueOf(time + i);   }      public Object clone(){           try{   return super.clone();       }catch(CloneNotSupportedException e)        {   return null;}   }   /*public String getGenID(String id)    {           double start = System.currentTimeMillis();        double end ;        String timem;        int i;        int j;        String tempid = new String();        java.security.SecureRandom  rd = new java.security.SecureRandom();        tempid = setTemp(String.valueOf( (double)(Math.random()*10000000000.0)));        if(this.hospital_site!=null)            id = id + hospital_site + tempid;         else            id = id + "00000" + tempid;         return id;    }*/   public String getGenID(String id) {//        String tempid = String.valueOf(System.currentTimeMillis());        java.security.SecureRandom  rd = new java.security.SecureRandom();        String tempid = setTemp(String.valueOf( (double)(Math.random()*10000000000.0)));        //        Constant.println("tempid" + tempid);        if(this.hospital_site!=null)            id = id + hospital_site + tempid;         else            id = id + "00000" + tempid;         return id;    }     public String getGenStandardID(String id) {                double start = System.currentTimeMillis();        double end ;        String timem;        int i;        int j;//        String tempid = String.valueOf(System.currentTimeMillis());        java.security.SecureRandom  rd = new java.security.SecureRandom();        String tempid = setTemp(String.valueOf( (double)(Math.random()*10000000000.0)));            id = id + tempid;         return id;    }    private String setTemp(String temp){          DecimalFormat df = new DecimalFormat();        df.applyPattern("0000000000");         temp = df.format(Double.parseDouble(temp));        return temp;    }     public static void main(String[] argc){    }}